# Repository conventions

这个仓库是个人读书笔记与 GitHub Pages 博客。

## 每次读书后的固定流程

1. 更新 `data/books.json` 中的阅读状态、日期和摘要。
2. 在 `notes/<slug>.md` 补充观点、论据、论证、反方观点和个人结论。
3. 在 `discussions/<slug>.md` 追加真实讨论；不要杜撰用户观点或阅读进度。
4. 运行 `scripts/build.py`，确认 `docs/index.html` 与对应书页更新。
5. 运行 `scripts/check.py`，通过后再提交和推送。

## 写作规则

- 明确区分：作者主张、书内证据、外部证据、个人判断。
- 对批量整理但未逐章精读的内容标记“结构化初读”，不能写成“读完”。
- 技术结论优先引用原作者、官方文档、标准或原始论文。
- 不大段摘录受版权保护的原文。
- 保留异议与适用边界，不把经验法则包装成普适定律。

## 版权规则

- 禁止把受版权保护的整本电子书提交到公开仓库。
- 私人副本放在 `library/`；该目录默认被 Git 忽略。
- `sources/catalog.json` 可以记录文件校验值与合法购买/开放获取链接。
- 只有公版或许可证明确允许再分发时，才可提交原文，并同时提交许可证说明。
