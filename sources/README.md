# 阅读源文件说明

原始电子书放在仓库根目录的 `library/` 中供本地阅读。该目录被 `.gitignore` 排除，不会上传到公开 GitHub 仓库。

公开的 `catalog.json` 记录：

- 对应书目 slug；
- 本地文件名与格式；
- 文件大小与 SHA-256；
- 是否允许公开再分发；
- 出版社、作者或开放获取页面。

这样既能确认笔记依据的是哪个版本，又不会把第三方版权内容重新公开发布。
